node_modules/
dist/
.astro/
.DS_Store

# Secrets — never commit real env files (keep .env.example)
.env
**/.env

# Generated member wiki content: contains real member names / care maps.
# Regenerated from the Obsidian vault by scripts/generate_members_wiki.py — do NOT
# commit it (this repo is public).
public/members/

# Python bytecode
__pycache__/
*.pyc

# Members-gated source docs — kept OUT of the public repo (they render into the
# password-gated /members/ area). Stored locally; back up privately, not here.
scripts/members_assets/
